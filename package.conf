name="weather"
description="Weather Information - Current conditions and forecasts"
version="0.1.0"
method="stow"
collection="system"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=("curl" "jq")
ignore=(
    "README.md"
)
